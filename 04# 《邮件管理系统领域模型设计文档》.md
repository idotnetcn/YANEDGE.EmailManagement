## 🤖 Assistant

# 《邮件管理系统领域模型设计文档》
## 基于最终版数据库设计的 DDD 领域模型方案
## 适用技术栈
- `.NET 10.x`
- `ABP 10.x`
- `PostgreSQL`

---

# 1. 文档目标

本文档用于在**最终版数据库设计**基础上，进一步给出邮件管理系统的**领域模型设计**，用于指导以下工作：

- 领域划分与限界上下文设计
- 聚合根、实体、值对象建模
- 领域服务设计
- 仓储接口边界
- 领域事件设计
- 应用服务与领域层职责分离
- 与 ABP 架构落地方式对齐
- 支撑共享邮箱、协同处理、审批、模板、规则、集成等核心业务

---

# 2. 设计原则

---

## 2.1 核心设计原则

本系统采用以下领域建模原则：

1. **以业务语义优先，而不是以表结构优先**
2. **聚合控制一致性边界**
3. **跨聚合使用 ID 引用，而不是直接对象导航**
4. **用户态、协同态、全局态分离**
5. **功能权限与数据权限分离**
6. **写模型与读模型可解耦**
7. **高频日志、统计、检索优先采用查询模型，不强塞进聚合**

---

## 2.2 建模重点

邮件系统表面上是“收发邮件”，但企业场景下实际上包含多类复杂业务：

- 多租户 SaaS 隔离
- 个人邮箱 / 共享邮箱 / 部门邮箱 / 系统邮箱
- 收件、发件、草稿、归档
- 线程协同
- 分派、认领、评论、审批
- 用户个性化状态
- 邮件模板、签名、规则
- 业务对象关联
- 第三方系统集成
- 审计留痕与统计分析

所以领域模型不能只围绕 `MailMessage` 一个对象展开，而应该进行**限界上下文拆分**。

---

# 3. 统一语言（Ubiquitous Language）

为了避免开发过程中语义混乱，先定义统一语言。

---

## 3.1 核心术语

### 租户 `Tenant`
SaaS 企业边界。  
一个租户下有多个用户、邮箱、规则、模板、权限配置。

### 邮箱账号 `MailAccount`
系统接入的一个邮箱实体，可为个人、共享、部门或系统邮箱。

### 邮箱归属 `Mail Account Ownership`
邮箱的主要归属主体，例如某用户、角色、组织或系统。

### 邮箱授权 `Mail Account Permission`
某用户/角色/组织对邮箱的查看、发送、管理、分派等权限。

### 邮件线程 `MailThread`
围绕同一主题或同一 `Message-Id / References` 形成的会话聚合视图。

### 邮件 `MailMessage`
单封邮件本体，包含发件人、主题、正文、收件人、附件等核心信息。

### 邮件正文 `MailMessageBody`
邮件正文内容，通常与主信息分离存储。

### 用户态 `User Message State`
某个用户对某封邮件的个性化状态，如已读、星标、隐藏、归档。

### 分派 `Assignment`
将线程或邮件处理责任转交给用户/角色/组织。

### 评论 `Internal Comment`
内部处理备注，不对外发送。

### 审批 `Approval`
某封待发邮件在发送前需要经过的审核流程。

### 模板 `Template`
邮件草稿模板，可包含主题模板、正文模板和变量。

### 规则 `Rule`
邮件进入系统后自动执行的条件与动作逻辑。

### 业务对象关联 `Business Relation`
邮件/线程与 CRM、ERP、工单、订单等业务实体的关联关系。

### 发件任务 `Send Task`
异步发信执行单元。

### 同步任务 `Sync Task`
从外部邮箱服务拉取邮件的执行单元。

### 审计日志 `Audit Log`
记录关键业务操作的不可抵赖日志。

---

# 4. 限界上下文划分

根据业务复杂度，建议拆分为以下限界上下文。

---

# 4.1 邮箱接入上下文 `Mail Account Context`

负责：

- 邮箱账号注册
- 邮箱配置维护
- 凭据管理
- 收发能力配置
- 邮箱健康状态
- 邮箱归属与授权

核心聚合：

- `MailAccount`

---

# 4.2 邮件会话上下文 `Mail Conversation Context`

负责：

- 邮件线程
- 邮件接收与存储
- 邮件正文、收件人、头信息、附件元数据
- 邮件用户态
- 文件夹映射
- 查询邮件详情

核心聚合：

- `MailThread`
- `MailMessage`（作为线程内实体或独立聚合的选择，见后文）

---

# 4.3 协同处理上下文 `Mail Collaboration Context`

负责：

- 分派
- 认领
- 协同参与
- 评论
- 待办
- 审批
- 处理状态流转

核心聚合：

- `MailWorkItem`（逻辑聚合概念，可由 `MailThread` 驱动）
- `MailApproval`

---

# 4.4 模板上下文 `Mail Template Context`

负责：

- 模板管理
- 模板版本
- 模板变量
- 签名管理

核心聚合：

- `MailTemplate`
- `MailSignature`

---

# 4.5 规则上下文 `Mail Rule Context`

负责：

- 自动分类
- 自动标记
- 自动分派
- 自动归档
- 自动绑定业务对象
- 域名黑白名单策略

核心聚合：

- `MailRule`
- `MailDomainPolicy`

---

# 4.6 业务关联上下文 `Business Relation Context`

负责：

- 联系人
- 业务对象类型
- 邮件与业务对象绑定关系

核心聚合：

- `Contact`
- `BusinessRelation`

---

# 4.7 发件与同步上下文 `Mail Transport Context`

负责：

- 发件任务调度
- 重试机制
- 外部发件反馈
- 同步任务调度
- 拉取执行日志

核心聚合：

- `MailSendTask`
- `MailSyncTask`

---

# 4.8 集成上下文 `Integration Context`

负责：

- 外部应用接入
- Webhook
- 幂等控制
- 外部映射关系
- 集成事件日志

核心聚合：

- `IntegrationApp`

---

# 4.9 审计与统计上下文 `Audit & Analytics Context`

负责：

- 审计日志
- 配置变更日志
- 统计日报

说明：  
这一部分更偏查询/追踪，不建议承载复杂领域规则。

---

# 5. 领域层分层建议

推荐采用以下分层结构：

```text
MailManagement.Domain
 ├─ Accounts
 ├─ Conversations
 ├─ Collaboration
 ├─ Templates
 ├─ Rules
 ├─ BusinessRelations
 ├─ Transport
 ├─ Integrations
 ├─ Auditing
 └─ Shared
```

每个上下文内部建议包含：

```text
- Aggregates
- Entities
- ValueObjects
- DomainServices
- Events
- Specifications
- Repositories
- Managers
```

---

# 6. 聚合设计总览

---

## 6.1 聚合根一览

| 聚合根 | 所属上下文 | 作用 |
|---|---|---|
| `MailAccount` | 邮箱接入 | 邮箱配置、归属、授权入口 |
| `MailThread` | 邮件会话 | 线程及其协同入口 |
| `MailApproval` | 协同处理 | 审批流程实体根 |
| `MailTemplate` | 模板 | 模板与版本管理 |
| `MailSignature` | 模板 | 签名管理 |
| `MailRule` | 规则 | 自动规则定义 |
| `MailDomainPolicy` | 规则 | 域名黑白名单策略 |
| `Contact` | 业务关联 | 联系人主数据 |
| `BusinessRelation` | 业务关联 | 邮件与业务对象关联 |
| `MailSendTask` | 发件与同步 | 发件任务状态机 |
| `MailSyncTask` | 发件与同步 | 邮件同步任务状态机 |
| `IntegrationApp` | 集成 | 外部应用管理 |

---

## 6.2 非聚合根实体一览

| 实体 | 归属聚合 |
|---|---|
| `MailAccountCredential` | `MailAccount` |
| `MailAccountSyncPolicy` | `MailAccount` |
| `MailAccountUserPermission` | `MailAccount` |
| `MailAccountRolePermission` | `MailAccount` |
| `MailAccountOuPermission` | `MailAccount` |
| `MailMessage` | `MailThread` 或独立查询模型 |
| `MailMessageBody` | `MailMessage` |
| `MailRecipient` | `MailMessage` |
| `MailHeader` | `MailMessage` |
| `MailAttachment` | `MailMessage` |
| `ThreadParticipant` | `MailThread` |
| `AssignmentRecord` | `MailThread` |
| `InternalComment` | `MailThread` |
| `ProcessingRecord` | `MailThread` |
| `MailTodo` | `MailThread` |
| `TemplateVersion` | `MailTemplate` |
| `RuleCondition` | `MailRule` |
| `RuleAction` | `MailRule` |

---

# 7. 聚合详细设计

---

# 7.1 `MailAccount` 聚合

---

## 7.1.1 聚合职责

`MailAccount` 是邮箱接入与授权的核心聚合根，负责：

- 定义邮箱类型
- 管理邮箱归属
- 启用/停用收发能力
- 管理同步策略
- 管理凭据
- 管理邮箱级授权
- 决定哪些主体可访问此邮箱

---

## 7.1.2 聚合根属性建议

### 基本属性
- `Id`
- `TenantId`
- `Name`
- `EmailAddress`
- `DisplayName`
- `AccountType`
- `Status`

### 归属属性
- `OwnerType`
- `OwnerUserId`
- `OwnerRoleId`
- `OwnerOrganizationUnitId`
- `IsShared`
- `VisibilityScope`
- `RequiresExplicitAuthorization`

### 收发配置
- `InboundProtocol`
- `OutboundProtocol`
- `ImapHost`
- `ImapPort`
- `SmtpHost`
- `SmtpPort`
- `UseSsl`
- `SyncEnabled`
- `SendEnabled`
- `SyncIntervalSeconds`

### 策略
- `AllowDelegateSend`
- `AllowDelegateManage`
- `DailySendLimit`

### 内部实体
- `Credential`
- `SyncPolicy`
- `UserPermissions`
- `RolePermissions`
- `OuPermissions`

---

## 7.1.3 聚合内实体

### `MailAccountCredential`
负责邮箱认证资料。

字段语义：
- 认证方式
- 用户名
- 加密密码
- 加密 AccessToken / RefreshToken
- 过期时间

### `MailAccountSyncPolicy`
定义同步范围与同步策略。

### `MailAccountUserPermission`
用户级数据权限。

### `MailAccountRolePermission`
角色级数据权限。

### `MailAccountOuPermission`
组织级数据权限。

---

## 7.1.4 核心业务规则

1. 一个邮箱必须属于某个归属主体或具有明确公开策略
2. 个人邮箱默认不应被标记为 `IsShared = true`
3. 若 `OwnerType = User`，则 `OwnerUserId` 必须存在
4. 若邮箱禁用发送，则不能创建有效发件任务
5. 若邮箱禁用同步，则不能调度同步任务
6. 权限授权有效期必须满足 `ValidFrom <= ValidTo`
7. 同一邮箱对同一用户只能有一条有效直接授权记录
8. Owner 默认具备最高优先级的邮箱权限

---

## 7.1.5 领域行为

### 创建邮箱
```text
CreateAccount(...)
```

### 修改基础信息
```text
ChangeProfile(...)
```

### 配置 IMAP / SMTP
```text
ConfigureInbound(...)
ConfigureOutbound(...)
```

### 配置凭据
```text
SetCredential(...)
RefreshToken(...)
```

### 启用 / 停用同步
```text
EnableSync()
DisableSync()
```

### 启用 / 停用发件
```text
EnableSend()
DisableSend()
```

### 设置同步策略
```text
SetSyncPolicy(...)
```

### 授权用户
```text
GrantUserPermission(userId, permission)
RevokeUserPermission(userId)
```

### 授权角色 / 组织
```text
GrantRolePermission(roleId, permission)
GrantOuPermission(ouId, permission)
```

### 更新所有者
```text
ChangeOwner(...)
```

---

## 7.1.6 推荐领域事件

- `MailAccountCreatedEvent`
- `MailAccountCredentialChangedEvent`
- `MailAccountSyncPolicyChangedEvent`
- `MailAccountPermissionGrantedEvent`
- `MailAccountPermissionRevokedEvent`
- `MailAccountOwnerChangedEvent`
- `MailAccountEnabledEvent`
- `MailAccountDisabledEvent`

---

# 7.2 `MailThread` 聚合

---

## 7.2.1 聚合定位

`MailThread` 是**邮件协同处理的核心聚合根**。  
企业邮件系统真正的处理对象往往不是单封邮件，而是“线程”。

它负责：

- 管理线程主信息
- 接纳新邮件
- 维护当前负责人
- 管理线程参与者
- 管理分派、评论、待办、处理轨迹
- 控制线程级状态流转

---

## 7.2.2 为什么以 `MailThread` 作为核心聚合

因为以下业务规则天然在线程层成立：

- 负责人通常是线程负责人，而不是单封邮件负责人
- 评论一般是面向线程
- 待办通常围绕线程
- 邮件来回往复属于同一业务协作单元
- 业务对象绑定通常也是线程级更自然

因此：

- **写模型**建议以 `MailThread` 为主
- `MailMessage` 在写模型中可作为线程下实体管理
- 若消息量极大，也可在实现层拆为独立仓储，但业务一致性仍围绕线程

---

## 7.2.3 聚合属性建议

### 线程基本信息
- `Id`
- `TenantId`
- `MailAccountId`
- `Subject`
- `NormalizedSubject`
- `ThreadKey`
- `LatestMessageTime`
- `LatestSender`
- `MessageCount`
- `Status`
- `Priority`

### 协同信息
- `OwnerUserId`
- `AssignedOrganizationUnitId`
- `AssignedRoleId`
- `AssignmentTime`
- `LastAssignmentUserId`

### 生命周期
- `FirstMessageTime`
- `ClosedTime`
- `ArchiveTime`

### 子实体集合
- `Messages`
- `Participants`
- `Assignments`
- `Comments`
- `ProcessingRecords`
- `Todos`

---

## 7.2.4 聚合内实体

### `MailMessage`
单封邮件实体。

### `ThreadParticipant`
线程参与者实体。

### `AssignmentRecord`
分派记录实体。

### `InternalComment`
内部评论实体。

### `ProcessingRecord`
状态变更记录。

### `MailTodo`
待办实体。

---

## 7.2.5 `MailMessage` 实体属性建议

### 本体信息
- `Id`
- `MailAccountId`
- `ThreadId`
- `FolderType`
- `InternetMessageId`
- `InReplyTo`
- `ReferencesText`
- `Subject`
- `FromAddress`
- `FromName`
- `SenderAddress`
- `ReplyTo`

### 时间与方向
- `SentTime`
- `ReceivedTime`
- `MailDirection`

### 状态
- `ProcessingStatus`
- `ApprovalStatus`
- `Importance`
- `HasAttachments`
- `IsLocked`
- `LockUserId`
- `LockTime`
- `SecurityLevel`
- `VisibilityPolicy`

### 操作人
- `CreatedByUserId`
- `SubmittedByUserId`
- `SentByUserId`
- `LastOperatorUserId`

### 子实体
- `Body`
- `Recipients`
- `Headers`
- `Attachments`

---

## 7.2.6 `MailMessage` 不负责的事情

以下不建议放进 `MailMessage` 实体内部：

- 某用户是否已读
- 某用户是否星标
- 某用户是否归档
- 某用户是否隐藏

这些属于**用户态上下文**，建议通过 `MailUserMessageState` 单独管理，不污染线程聚合的一致性边界。

---

## 7.2.7 `MailThread` 核心业务规则

1. 同一线程必须属于同一邮箱账号
2. 线程必须至少拥有一封邮件
3. 线程负责人可为空，但线程关闭前应明确负责人或关闭原因
4. 已关闭线程不能再直接新增评论/待办，除非重新打开
5. 已归档线程不能再接受普通协同操作
6. 受限可见邮件只有线程参与者/负责人/审批人可查看
7. 线程分派后需同步更新当前负责人
8. 新邮件进入线程时，应更新 `LatestMessageTime`、`LatestSender`、`MessageCount`
9. 被锁定邮件不能被其他用户重复编辑草稿或发送
10. 草稿发送前若需要审批，则必须先进入审批通过状态

---

## 7.2.8 领域行为

### 创建线程
```text
CreateThread(accountId, subject, threadKey, firstMessage)
```

### 增加新邮件
```text
AppendMessage(message)
```

### 指派负责人
```text
AssignToUser(fromUserId, toUserId, comment)
AssignToRole(fromUserId, roleId, comment)
AssignToOrganizationUnit(fromUserId, ouId, comment)
```

### 认领线程
```text
Claim(userId)
```

### 重新打开线程
```text
Reopen(userId, reason)
```

### 关闭线程
```text
Close(userId, reason)
```

### 归档线程
```text
Archive(userId)
```

### 添加参与者
```text
AddParticipant(userId, participantType)
```

### 添加评论
```text
AddComment(userId, content, mentions)
```

### 添加待办
```text
AddTodo(relatedUserId, title, dueTime)
```

### 标记状态
```text
ChangeProcessingStatus(messageId, newStatus, operatorUserId, description)
```

### 锁定邮件
```text
LockMessage(messageId, userId)
UnlockMessage(messageId, userId)
```

---

## 7.2.9 推荐领域事件

- `MailThreadCreatedEvent`
- `MailMessageAppendedEvent`
- `MailThreadAssignedEvent`
- `MailThreadClaimedEvent`
- `MailThreadClosedEvent`
- `MailThreadReopenedEvent`
- `MailThreadArchivedEvent`
- `MailCommentAddedEvent`
- `MailTodoCreatedEvent`
- `MailProcessingStatusChangedEvent`
- `MailMessageLockedEvent`
- `MailMessageUnlockedEvent`
- `MailParticipantAddedEvent`

---

# 7.3 `MailApproval` 聚合

---

## 7.3.1 聚合职责

用于管理需要审批的邮件发送流程。

职责：

- 提交审批
- 指定审批人
- 审批通过/拒绝
- 记录审批意见
- 驱动后续发件任务

---

## 7.3.2 聚合属性
- `Id`
- `TenantId`
- `MailMessageId`
- `ApprovalCode`
- `FlowName`
- `NodeName`
- `ApproverUserId`
- `ApprovalStatus`
- `SubmittedUserId`
- `SubmittedTime`
- `ProcessedTime`
- `Comment`

---

## 7.3.3 核心规则

1. 一条审批必须对应一封邮件
2. 已审批通过的审批单不能再次拒绝
3. 已拒绝审批单不能直接发送，除非重新提交
4. 审批人不能为空
5. 提交审批后邮件状态应进入 `待审批`
6. 审批通过后方可进入发件调度
7. 已撤回审批单不能再处理

---

## 7.3.4 领域行为

```text
Submit(messageId, submittedUserId, approverUserId)
Approve(approverUserId, comment)
Reject(approverUserId, comment)
Withdraw(submittedUserId, reason)
```

---

## 7.3.5 领域事件

- `MailApprovalSubmittedEvent`
- `MailApprovalApprovedEvent`
- `MailApprovalRejectedEvent`
- `MailApprovalWithdrawnEvent`

---

# 7.4 `MailTemplate` 聚合

---

## 7.4.1 聚合职责

负责模板及版本管理：

- 创建模板
- 维护模板元数据
- 新建版本
- 发布版本
- 管理变量
- 生成渲染输入

---

## 7.4.2 聚合属性
- `Id`
- `TenantId`
- `Code`
- `Name`
- `CategoryId`
- `BodyFormat`
- `LanguageCode`
- `CurrentVersionNo`
- `IsEnabled`
- `NeedApproval`
- `Description`

### 子实体
- `Versions`

---

## 7.4.3 `TemplateVersion` 实体
- `VersionNo`
- `SubjectTemplate`
- `BodyContent`
- `ChangeSummary`
- `IsPublished`
- `PublisherId`
- `PublishTime`

---

## 7.4.4 规则

1. 同一模板编码唯一
2. 版本号必须递增
3. 同一时刻只能有一个当前发布版本
4. 禁用模板不能用于新草稿生成
5. 含变量模板必须通过变量解析器验证

---

## 7.4.5 领域行为

```text
CreateTemplate(...)
AddVersion(...)
PublishVersion(versionNo, publisherId)
Disable()
Enable()
Rename(...)
ChangeCategory(...)
```

---

## 7.4.6 领域事件

- `MailTemplateCreatedEvent`
- `MailTemplateVersionAddedEvent`
- `MailTemplateVersionPublishedEvent`
- `MailTemplateDisabledEvent`

---

# 7.5 `MailSignature` 聚合

---

## 7.5.1 聚合职责
管理用户、角色、组织等维度的签名。

---

## 7.5.2 属性
- `Id`
- `TenantId`
- `Name`
- `OwnerType`
- `OwnerId`
- `BodyFormat`
- `Content`
- `IsDefault`
- `IsEnabled`

---

## 7.5.3 行为
```text
SetAsDefault()
Disable()
Enable()
ChangeContent(...)
```

---

# 7.6 `MailRule` 聚合

---

## 7.6.1 聚合职责

规则聚合负责描述“满足什么条件，执行什么动作”。

---

## 7.6.2 聚合属性
- `Id`
- `TenantId`
- `Name`
- `RuleType`
- `Priority`
- `IsEnabled`
- `StopOnMatch`
- `AppliesToAccountId`
- `AppliesToFolderType`
- `Description`

### 子实体
- `Conditions`
- `Actions`

---

## 7.6.3 `RuleCondition` 实体
- `FieldName`
- `Operator`
- `CompareValue`
- `LogicalOperator`
- `SortOrder`

## 7.6.4 `RuleAction` 实体
- `ActionType`
- `ActionValue`
- `ActionOrder`

---

## 7.6.5 核心规则

1. 规则至少有一个条件
2. 规则至少有一个动作
3. 优先级数值越小，执行顺序越靠前
4. 禁用规则不可参与执行
5. `StopOnMatch = true` 时，命中后应中断后续规则链
6. 作用于某邮箱的规则不能跨邮箱错误应用
7. 条件字段与动作类型必须在系统允许清单内

---

## 7.6.6 领域行为

```text
CreateRule(...)
AddCondition(...)
RemoveCondition(...)
AddAction(...)
RemoveAction(...)
Enable()
Disable()
ChangePriority(...)
```

---

## 7.6.7 领域事件

- `MailRuleCreatedEvent`
- `MailRuleEnabledEvent`
- `MailRuleDisabledEvent`
- `MailRuleChangedEvent`

---

# 7.7 `MailDomainPolicy` 聚合

---

## 7.7.1 职责
管理域名黑名单、白名单、可信域规则。

---

## 7.7.2 属性
- `Id`
- `TenantId`
- `DomainName`
- `PolicyType`
- `IsEnabled`
- `Description`

---

## 7.7.3 行为
```text
Enable()
Disable()
RenameDescription(...)
```

---

# 7.8 `Contact` 聚合

---

## 7.8.1 职责
联系人主数据管理。

---

## 7.8.2 属性
- `Id`
- `TenantId`
- `ContactType`
- `Name`
- `EmailAddress`
- `Mobile`
- `CompanyName`
- `ExternalId`
- `SourceSystem`
- `DepartmentName`
- `Title`
- `Remark`
- `IsEnabled`

---

## 7.8.3 规则

1. 同租户内可允许同邮箱多联系人，但建议由应用层做查重提示
2. 若来源于外部系统，`ExternalId + SourceSystem` 建议唯一
3. 禁用联系人不能参与自动推荐

---

## 7.8.4 行为
```text
ChangeProfile(...)
Disable()
Enable()
BindExternalIdentity(...)
```

---

# 7.9 `BusinessRelation` 聚合

---

## 7.9.1 职责
负责邮件 / 线程 与业务对象的绑定关系。

---

## 7.9.2 属性
- `Id`
- `TenantId`
- `MailMessageId`
- `ThreadId`
- `RelationType`
- `BusinessObjectType`
- `BusinessObjectId`
- `BusinessObjectNo`
- `BusinessObjectName`
- `IsPrimary`
- `SourceType`
- `SourceSystem`

---

## 7.9.3 规则

1. 必须至少绑定到 `MailMessageId` 或 `ThreadId` 之一
2. 同一业务对象可与多个线程关联
3. 同一线程在同一业务对象类型下可存在一个主关联
4. 手工关联与系统规则关联应保留来源区分

---

## 7.9.4 行为
```text
BindToThread(...)
BindToMessage(...)
MarkAsPrimary()
Unbind()
```

---

# 7.10 `MailSendTask` 聚合

---

## 7.10.1 聚合职责

这是发件执行状态机聚合，负责：

- 创建发件任务
- 调度发送
- 重试
- 失败记录
- 成功回写

---

## 7.10.2 聚合属性
- `Id`
- `TenantId`
- `MailAccountId`
- `MailMessageId`
- `TaskNo`
- `SendStatus`
- `RetryCount`
- `MaxRetryCount`
- `ScheduledSendTime`
- `LastSendTime`
- `NextRetryTime`
- `LastErrorCode`
- `LastErrorMessage`
- `TriggeredByType`
- `ExternalRequestId`
- `ExternalSourceSystem`
- `CreatedByUserId`
- `TriggeredByUserId`
- `ApprovedByUserId`
- `CancelledByUserId`
- `OperatorUserId`

---

## 7.10.3 核心规则

1. 只有具备发送能力的邮箱可创建发件任务
2. 若邮件需要审批，则审批通过前不能转为待发送
3. 已成功发送任务不能再次发送
4. 重试次数不可超过最大重试数
5. 已取消任务不能重新进入发送中
6. 外部请求幂等键在同一来源系统下必须唯一

---

## 7.10.4 领域行为

```text
Create(...)
Schedule(sendTime)
StartSending(operatorUserId)
MarkSuccess(sendTime)
MarkFailure(errorCode, errorMessage, nextRetryTime)
Cancel(userId, reason)
```

---

## 7.10.5 领域事件

- `MailSendTaskCreatedEvent`
- `MailSendTaskScheduledEvent`
- `MailSendTaskStartedEvent`
- `MailSendTaskSucceededEvent`
- `MailSendTaskFailedEvent`
- `MailSendTaskCancelledEvent`

---

# 7.11 `MailSyncTask` 聚合

---

## 7.11.1 职责
表示一次邮箱同步执行单元。

---

## 7.11.2 属性
- `Id`
- `TenantId`
- `MailAccountId`
- `FolderType`
- `SyncStatus`
- `SyncStartTime`
- `SyncEndTime`
- `FetchedCount`
- `InsertedCount`
- `UpdatedCount`
- `SkippedCount`
- `FailedCount`
- `LastUid`
- `ErrorMessage`

---

## 7.11.3 规则

1. 同一邮箱同一时刻不应存在多个冲突中的同步任务
2. 禁用同步的邮箱不可创建同步任务
3. 同步完成后必须写回账户最后同步时间
4. 失败不等于全局失败，可允许部分成功

---

## 7.11.4 行为

```text
Start()
MarkPartialSuccess(...)
MarkSuccess(...)
MarkFailure(error)
```

---

## 7.11.5 领域事件

- `MailSyncTaskStartedEvent`
- `MailSyncTaskSucceededEvent`
- `MailSyncTaskFailedEvent`
- `MailSyncTaskPartiallySucceededEvent`

---

# 7.12 `IntegrationApp` 聚合

---

## 7.12.1 职责
外部应用接入管理。

---

## 7.12.2 属性
- `Id`
- `TenantId`
- `AppId`
- `AppName`
- `EncryptedAppSecret`
- `SourceSystem`
- `CallbackUrl`
- `IsEnabled`
- `AllowedIps`

---

## 7.12.3 行为
```text
RotateSecret(...)
Enable()
Disable()
ChangeCallbackUrl(...)
ChangeAllowedIps(...)
```

---

# 8. 值对象设计

---

## 8.1 为什么要使用值对象

以下信息明显更适合作为值对象而不是裸字段：

- 邮箱地址
- 邮件主题规范化
- 凭据
- 服务器配置
- 授权能力集合
- 审批意见
- 错误信息
- 域名策略

值对象可以更好地承载：

- 格式校验
- 语义表达
- 不可变性
- 重用

---

# 8.2 推荐值对象一览

| 值对象 | 用途 |
|---|---|
| `EmailAddress` | 标准邮箱地址 |
| `MailAddressInfo` | 邮箱地址 + 显示名 |
| `MailServerConfig` | IMAP/SMTP 配置 |
| `MailCredential` | 认证信息 |
| `MailPermissionSet` | 授权能力集合 |
| `ThreadSubject` | 主题与规范化主题 |
| `MailBodyContent` | 正文内容 |
| `MailRecipientInfo` | 收件人 |
| `AttachmentInfo` | 附件元信息 |
| `ApprovalDecision` | 审批结果与意见 |
| `ScheduleTime` | 定时发送时间 |
| `ErrorInfo` | 错误码与错误信息 |
| `BusinessObjectRef` | 业务对象引用 |
| `DomainRuleValue` | 域名策略对象 |

---

## 8.3 示例：`EmailAddress`

```text
属性：
- Address
- NormalizedAddress

规则：
- 不可为空
- 必须是有效邮箱格式
- 自动转小写标准化
```

---

## 8.4 示例：`MailPermissionSet`

```text
属性：
- CanView
- CanSend
- CanManage
- CanAssign
- CanDownloadAttachment
- CanViewSensitive
- CanDeleteMail
- CanArchiveMail
```

它可以用于：

- 用户授权
- 角色授权
- 组织授权
- 最终权限合成

---

## 8.5 示例：`MailAddressInfo`

```text
属性：
- EmailAddress
- DisplayName
```

用于：
- 发件人
- 收件人
- Reply-To
- Sender

---

# 9. 领域服务设计

某些业务逻辑跨聚合，且不属于单一实体职责，应抽为领域服务。

---

# 9.1 `MailAccountPermissionResolver`

## 职责
解析用户对某邮箱的最终权限。

## 输入
- `mailAccountId`
- `userId`

## 可能依赖
- 用户授权仓储
- 角色授权仓储
- 组织授权仓储
- 用户角色/组织查询接口
- 系统管理员判断接口

## 输出
- `ResolvedMailPermission`

## 规则
合成以下来源：
- 用户显式授权
- 角色继承
- 组织继承
- Owner 默认权限
- 管理员覆盖权限

---

# 9.2 `MailThreadVisibilityChecker`

## 职责
判断某用户是否可查看某线程或某邮件。

## 规则考虑：
- 是否有邮箱查看权限
- 邮件 `VisibilityPolicy`
- 是否是线程参与者
- 是否是当前负责人
- 是否在分派链中
- 是否是审批人
- 邮件密级是否匹配权限

---

# 9.3 `MailMessageImportDomainService`

## 职责
从外部同步结果生成领域对象。

## 作用
- 识别线程
- 创建或追加 `MailThread`
- 创建 `MailMessage`
- 落地收件人/附件/头信息
- 触发规则执行
- 初始化用户态

---

# 9.4 `MailRuleEngineDomainService`

## 职责
执行规则匹配。

## 作用
- 加载可用规则
- 评估条件
- 生成动作计划
- 执行动作（打标签、分派、归档、绑定业务对象等）

---

# 9.5 `MailTemplateRenderDomainService`

## 职责
渲染模板。

## 输入
- 模板版本
- 变量字典
- 签名

## 输出
- 主题
- 正文

---

# 9.6 `MailSendPolicyDomainService`

## 职责
发件前业务校验。

## 校验点
- 邮箱是否允许发送
- 用户是否具备发送权限
- 日发送上限是否超限
- 邮件是否已通过审批
- 邮件是否已锁定且锁归属当前用户
- 目标域名是否受策略限制

---

# 9.7 `BusinessRelationBinderDomainService`

## 职责
根据邮件内容自动识别和绑定业务对象。

---

# 10. 仓储设计

---

## 10.1 仓储原则

1. 仓储只面向聚合根
2. 不为每张表都建立仓储
3. 查询复杂列表尽量走 QueryService / ReadModel
4. 不在仓储中混入应用层编排逻辑

---

## 10.2 推荐仓储接口

### `IMailAccountRepository`
```text
- GetAsync(id)
- FindByEmailAddressAsync(email)
- InsertAsync(account)
- UpdateAsync(account)
- DeleteAsync(account)
```

### `IMailThreadRepository`
```text
- GetAsync(id)
- FindByThreadKeyAsync(accountId, threadKey)
- InsertAsync(thread)
- UpdateAsync(thread)
```

### `IMailApprovalRepository`
```text
- GetAsync(id)
- FindByMessageIdAsync(messageId)
- InsertAsync(approval)
- UpdateAsync(approval)
```

### `IMailTemplateRepository`
### `IMailRuleRepository`
### `IContactRepository`
### `IBusinessRelationRepository`
### `IMailSendTaskRepository`
### `IMailSyncTaskRepository`
### `IIntegrationAppRepository`

---

## 10.3 查询模型建议不直接走仓储

以下查询建议单独做 QueryService：

- 收件箱分页列表
- 按条件检索邮件
- 我的未读邮件
- 我参与的线程
- 某业务对象下的全部邮件
- 审批待办列表
- 统计报表

因为这些查询往往跨多表、多条件，不适合聚合仓储。

---

# 11. 用户态模型设计

---

## 11.1 为什么用户态不放进线程聚合

`MailUserMessageState` 有以下特点：

- 以“用户+邮件”为主键语义
- 更新非常频繁
- 不影响线程核心一致性
- 更偏个性化偏好

如果强行放进 `MailThread` 聚合，会带来：

- 聚合过大
- 写冲突频繁
- 共享邮箱多人操作时锁争用严重

所以建议：

## 结论
`MailUserMessageState` 单独建模为**轻量聚合**或偏事务脚本式领域对象。

---

## 11.2 `MailUserMessageState` 领域对象职责

负责：
- 标记已读
- 标记未读
- 星标/取消星标
- 归档/取消归档
- 隐藏/取消隐藏
- 更新最后查看时间
- 设置稍后提醒

---

## 11.3 核心规则

1. 用户必须对所属邮箱有可见权限，才能创建/变更用户态
2. 首次标记已读时写 `FirstReadTime`
3. 每次查看都更新 `LastViewTime`
4. 用户态不应影响其他用户

---

# 12. 领域事件总览

---

## 12.1 事件设计原则

领域事件用于表示**业务上已发生的重要事实**，而不是简单技术通知。

---

## 12.2 事件分类

### 邮箱类
- `MailAccountCreatedEvent`
- `MailAccountPermissionGrantedEvent`
- `MailAccountPermissionRevokedEvent`
- `MailAccountOwnerChangedEvent`

### 邮件类
- `MailThreadCreatedEvent`
- `MailMessageAppendedEvent`
- `MailThreadAssignedEvent`
- `MailProcessingStatusChangedEvent`
- `MailThreadClosedEvent`
- `MailCommentAddedEvent`

### 审批类
- `MailApprovalSubmittedEvent`
- `MailApprovalApprovedEvent`
- `MailApprovalRejectedEvent`

### 发件类
- `MailSendTaskCreatedEvent`
- `MailSendTaskSucceededEvent`
- `MailSendTaskFailedEvent`

### 同步类
- `MailSyncTaskStartedEvent`
- `MailSyncTaskSucceededEvent`
- `MailSyncTaskFailedEvent`

### 模板类
- `MailTemplateVersionPublishedEvent`

### 规则类
- `MailRuleChangedEvent`

### 关联类
- `BusinessRelationBoundEvent`

---

## 12.3 典型事件链路示例

### 邮件同步进入系统
```text
MailSyncTaskStartedEvent
 -> 外部邮件拉取
 -> MailMessageImported
 -> MailThreadCreatedEvent / MailMessageAppendedEvent
 -> MailRuleEngineExecuted
 -> BusinessRelationBoundEvent
 -> MailSyncTaskSucceededEvent
```

### 草稿提审并发送
```text
MailApprovalSubmittedEvent
 -> 审批通过
 -> MailApprovalApprovedEvent
 -> MailSendTaskCreatedEvent
 -> MailSendTaskStartedEvent
 -> MailSendTaskSucceededEvent
```

### 共享邮箱分派处理
```text
MailThreadAssignedEvent
 -> 更新负责人
 -> 添加参与者
 -> 生成待办
 -> 审计记录
```

---

# 13. 应用服务与领域层边界

---

## 13.1 应用服务负责什么

应用服务负责：

- 鉴权
- 参数校验
- DTO 转换
- 调用领域服务 / 聚合行为
- 事务提交
- 发布集成事件
- 编排多个聚合

---

## 13.2 领域层负责什么

领域层负责：

- 业务规则
- 状态机
- 一致性约束
- 业务不变量
- 领域事件

---

## 13.3 不应该放在领域层的内容

- HTTP 上下文
- Controller 参数解析
- 前端分页模型
- SQL 拼接查询
- ABP 权限名字符串
- 第三方 SDK 直接调用细节
- 原始 IMAP 协议交互

这些应放在：

- Application 层
- Infrastructure 层
- Integration 层

---

# 14. 典型用例建模

---

# 14.1 用例：创建邮箱账号

## 应用服务流程
1. 校验用户是否具备邮箱管理权限
2. 调用 `MailAccount` 创建
3. 设置凭据、同步策略
4. 初始化 owner 权限
5. 保存聚合
6. 发布 `MailAccountCreatedEvent`

---

# 14.2 用例：同步邮件

## 流程
1. 创建 `MailSyncTask`
2. 调用基础设施层拉取外部邮件
3. 对每封外部邮件调用 `MailMessageImportDomainService`
4. 创建或追加 `MailThread`
5. 规则引擎执行
6. 保存结果
7. 更新同步任务状态
8. 发布同步完成事件

---

# 14.3 用例：共享邮箱分派邮件

## 流程
1. 校验当前用户对邮箱具备 `CanAssign`
2. 加载 `MailThread`
3. 调用 `AssignToUser(...)`
4. 更新线程负责人
5. 添加参与者
6. 生成待办
7. 记录处理轨迹与审计日志

---

# 14.4 用例：用户查看邮件

## 流程
1. 应用层先调用 `MailThreadVisibilityChecker`
2. 允许后读取详情 QueryModel
3. 更新 `MailUserMessageState`
4. 若首次已读则写首次阅读时间
5. 发布可选事件 `MailMessageViewedEvent`

---

# 14.5 用例：草稿发送

## 流程
1. 校验邮箱发送权限
2. 校验邮件是否锁定
3. 校验审批要求
4. 若需审批则创建 `MailApproval`
5. 若无需审批则创建 `MailSendTask`
6. 异步发送成功后更新状态

---

# 15. 聚合一致性边界与事务建议

---

## 15.1 强一致事务建议

以下操作建议在单事务内完成：

### 邮箱授权变更
- 更新 `MailAccount`
- 更新权限子实体

### 线程分派
- 更新 `MailThread` 负责人
- 插入分派记录
- 插入处理轨迹
- 插入参与者/待办

### 审批处理
- 更新 `MailApproval`
- 更新 `MailMessage.ApprovalStatus`

---

## 15.2 最终一致即可的场景

以下适合异步化：

- 发件成功后推送 webhook
- 邮件进入系统后自动绑定业务对象
- 邮件全文检索索引更新
- 统计日报更新
- 审计扩展写入
- 通知消息发送

---

# 16. 状态机设计

---

# 16.1 邮件处理状态机

```text
待分派 -> 待处理 -> 处理中 -> 待审批 -> 已完成 -> 已归档 -> 已关闭
```

允许部分逆向操作：

- `已关闭 -> 处理中`（重新打开）
- `待审批 -> 处理中`（审批拒绝或撤回）

---

# 16.2 审批状态机

```text
不需要审批
待审批 -> 审批通过
待审批 -> 审批拒绝
待审批 -> 已撤回
审批拒绝 -> 待审批（重新提交）
```

---

# 16.3 发件任务状态机

```text
待发送 -> 发送中 -> 成功
待发送 -> 发送中 -> 失败 -> 待发送/发送中（重试）
待发送 -> 已取消
```

---

# 16.4 同步任务状态机

```text
待执行 -> 同步中 -> 成功
待执行 -> 同步中 -> 失败
待执行 -> 同步中 -> 部分成功
```

---

# 17. 规格模式建议

对于复杂条件判断，可以引入 `Specification`。

---

## 17.1 推荐规格

### `CanAccessMailAccountSpecification`
判断用户是否可访问邮箱。

### `CanViewMailMessageSpecification`
判断用户是否可查看邮件。

### `CanAssignThreadSpecification`
判断用户是否可分派线程。

### `CanSendMailSpecification`
判断用户是否可发送邮件。

### `MailRequiresApprovalSpecification`
判断邮件是否需要审批。

### `CanApproveMailSpecification`
判断当前用户是否可审批。

---

# 18. 防腐层设计

如果系统需要对接外部平台，应通过 Anti-Corruption Layer 隔离协议细节。

---

## 18.1 需要防腐的外部系统

- IMAP / POP3 / SMTP 服务
- Exchange / Graph API
- CRM / ERP / 工单系统
- 外部审批系统
- 第三方通知平台

---

## 18.2 防腐层职责

- 协议对象转领域对象
- 外部错误码转内部错误语义
- 避免第三方 SDK 直接污染领域层

---

# 19. 与 ABP 的落地建议

---

## 19.1 聚合根基类建议

### 适合继承
- `FullAuditedAggregateRoot<Guid>`
- `AggregateRoot<Guid>`
- `Entity<Guid>`
- `ValueObject`

---

## 19.2 Domain Service 建议

可采用 ABP 的 `DomainService` 基类。

例如：

- `MailAccountManager`
- `MailThreadManager`
- `MailApprovalManager`
- `MailRuleEngine`
- `MailTemplateRenderManager`

---

## 19.3 Repository 建议

聚合根用仓储接口，复杂查询可单独建立：

- `IMailInboxQueryService`
- `IMailThreadQueryService`
- `IMailApprovalQueryService`

---

## 19.4 Domain Event 建议

使用 ABP 本地事件总线：
- `ILocalEventBus`

跨系统集成建议额外映射为集成事件：
- `MailSentIntegrationEvent`
- `MailReceivedIntegrationEvent`

---

# 20. 读写分离建议

---

## 20.1 为什么要读写分离

邮件列表、搜索、统计场景查询复杂、高频，而写模型更关注一致性。

---

## 20.2 写模型
围绕聚合根：
- `MailAccount`
- `MailThread`
- `MailApproval`
- `MailTemplate`
- `MailRule`
- `MailSendTask`
- `MailSyncTask`

---

## 20.3 读模型
面向页面展示与检索：

- 收件箱列表
- 线程详情
- 我的待办
- 我的审批
- 我的未读
- 联系人建议
- 统计报表

---

# 21. 模块间依赖建议

建议依赖方向如下：

```text
Accounts -> Shared
Conversations -> Accounts, Shared
Collaboration -> Conversations, Accounts, Shared
Templates -> Shared
Rules -> Conversations, BusinessRelations, Shared
BusinessRelations -> Shared
Transport -> Conversations, Accounts, Collaboration, Shared
Integrations -> Shared
Auditing -> Shared
```

避免：
- `Rules` 反向依赖 `Transport`
- `Templates` 依赖 `Audit`
- `Conversations` 依赖具体第三方 SDK

---

# 22. 领域对象示意结构

---

## 22.1 `MailAccount` 结构示意

```text
MailAccount
 ├─ MailAccountCredential
 ├─ MailAccountSyncPolicy
 ├─ List<MailAccountUserPermission>
 ├─ List<MailAccountRolePermission>
 └─ List<MailAccountOuPermission>
```

---

## 22.2 `MailThread` 结构示意

```text
MailThread
 ├─ List<MailMessage>
 │   ├─ MailMessageBody
 │   ├─ List<MailRecipient>
 │   ├─ List<MailHeader>
 │   └─ List<MailAttachment>
 ├─ List<ThreadParticipant>
 ├─ List<AssignmentRecord>
 ├─ List<InternalComment>
 ├─ List<ProcessingRecord>
 └─ List<MailTodo>
```

---

## 22.3 `MailTemplate` 结构示意

```text
MailTemplate
 └─ List<TemplateVersion>
```

---

## 22.4 `MailRule` 结构示意

```text
MailRule
 ├─ List<RuleCondition>
 └─ List<RuleAction>
```

---

# 23. 一期实现优先级建议

---

## 23.1 第一阶段必须先建好的领域模型

1. `MailAccount`
2. `MailThread`
3. `MailApproval`
4. `MailSendTask`
5. `MailSyncTask`
6. `MailTemplate`
7. `MailRule`
8. `MailUserMessageState`（轻量领域对象）

---

## 23.2 第二阶段补强

1. `Contact`
2. `BusinessRelation`
3. `MailSignature`
4. `MailDomainPolicy`
5. `IntegrationApp`

---

# 24. 最终结论

本领域模型方案的核心思想是：

## 24.1 邮箱是接入边界
邮箱归属、凭据、同步、授权，都由 `MailAccount` 聚合统一管理。

## 24.2 线程是协同边界
企业邮件系统的处理中心不是单封邮件，而是 `MailThread`。

## 24.3 用户态独立
已读、星标、隐藏、归档这类状态不属于线程全局状态，必须独立建模。

## 24.4 审批与收发分离
审批是业务决策，发件任务是技术执行，两者应拆分聚合。

## 24.5 规则与模板独立上下文
模板是内容生产工具，规则是自动化决策工具，不能混为一谈。

## 24.6 读写分离是必要的
复杂列表与搜索应走读模型，避免让聚合承担报表与检索职责。

---

# 25. 下一步建议

基于这份领域模型文档，下一步最适合继续输出的是以下之一：

1. **《ABP 实体设计与 EF Core FluentAPI 映射代码骨架》**
2. **《邮件管理系统应用服务设计文档》**
3. **《邮件管理系统权限模型设计文档》**
4. **《邮件管理系统领域事件与集成事件设计文档》**

如果按研发落地顺序，我最建议下一步继续输出：

## 《ABP 实体设计与 EF Core FluentAPI 映射代码骨架》

因为现在：
- 数据库模型已经有了
- 领域模型已经有了
- 下一步最自然就是实体类、聚合根、配置映射落代码

如果你要，我下一条可以直接继续输出：

# 《邮件管理系统 ABP 实体设计与 EF Core FluentAPI 映射代码骨架》
