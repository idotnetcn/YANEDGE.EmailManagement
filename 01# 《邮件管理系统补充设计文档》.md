## 🤖 Assistant

# 邮件管理系统补充设计文档（一期落地版）

> 基于：
>
> - `.NET 10.x`
> - `ABP 10.x`
> - `PostgreSQL`
>
> 目标：
>
> - 支持独立部署
> - 支持 ERP/CRM 集成
> - 模块化、易扩展、易维护
> - 为后续演进为 ERP/CRM 平台预留能力

---

# 1. 一期功能清单（菜单/功能点级别）

## 1.1 菜单结构建议

```text
首页工作台
├─ 我的待办
├─ 今日邮件统计
├─ 待审批邮件
├─ 待处理分派邮件
├─ 发件失败告警
├─ 最近协同动态

邮件中心
├─ 收件箱
├─ 已发送
├─ 草稿箱
├─ 归档箱
├─ 垃圾箱
├─ 共享邮箱
├─ 星标邮件
├─ 待处理邮件
├─ 我负责的邮件
├─ 我参与的邮件
├─ 全部线程

协同处理
├─ 邮件分派
├─ 内部备注
├─ 转派记录
├─ 处理状态管理
├─ 审批中心
├─ 协同日志

业务关联
├─ 联系人
├─ 客户关联邮件
├─ 供应商关联邮件
├─ 订单关联邮件
├─ 合同关联邮件
├─ 工单关联邮件
├─ 自定义业务对象关联

模板中心
├─ 邮件模板
├─ 模板分类
├─ 模板变量
├─ 邮件签名
├─ 模板版本记录

规则中心
├─ 自动分类规则
├─ 自动分派规则
├─ 黑名单/白名单
├─ 可信域配置
├─ 规则执行日志

附件中心
├─ 附件列表
├─ 附件预览
├─ 附件归档
├─ 附件权限记录

统计分析
├─ 邮件收发统计
├─ 邮箱账号统计
├─ 人员处理统计
├─ 响应时效统计
├─ 失败发送统计
├─ 规则命中统计

集成中心
├─ API 应用管理
├─ Webhook 配置
├─ 集成日志
├─ ERP/CRM 映射配置
├─ 外部业务对象同步

系统管理
├─ 组织机构
├─ 用户管理
├─ 角色管理
├─ 权限管理
├─ 邮箱账号管理
├─ 系统参数
├─ 通知配置
├─ 审计日志
├─ 操作日志
├─ 健康检查
```

---

## 1.2 功能点明细

## 1.2.1 首页工作台

### 功能点
- 展示当前用户待处理邮件数量
- 展示待审批邮件数量
- 展示发件失败任务数量
- 展示我的共享邮箱处理情况
- 展示最近邮件协同动态
- 展示快捷入口（写邮件、查看待办、查看分派）

### 权限点
- `Dashboard.View`
- `Mail.Todo.View`
- `Mail.Approval.View`

---

## 1.2.2 邮件中心

### 收件箱
- 查看邮件列表
- 条件筛选
- 查看邮件详情
- 标记已读/未读
- 打标签
- 回复/转发
- 分派
- 归档
- 删除
- 下载附件
- 批量操作

### 已发送
- 查询发件记录
- 查看发件状态
- 重发失败邮件
- 关联业务对象
- 查看线程上下文

### 草稿箱
- 保存草稿
- 编辑草稿
- 删除草稿
- 提交审批
- 发送

### 归档箱
- 查看归档邮件
- 恢复归档
- 导出归档
- 查看归档操作记录

### 共享邮箱
- 查看共享邮箱邮件
- 按负责人过滤
- 抢单/认领
- 转派
- 批量分派

### 权限点建议
- `Mail.Inbox.View`
- `Mail.Inbox.Read`
- `Mail.Send.Create`
- `Mail.Send.Resend`
- `Mail.Draft.Manage`
- `Mail.Archive.View`
- `Mail.Archive.Restore`
- `Mail.Delete`
- `Mail.Attachment.Download`

---

## 1.2.3 协同处理

### 功能点
- 邮件分派到用户/角色/部门
- 支持内部备注
- 支持协同处理人
- 支持处理状态流转
- 支持 SLA 超时提醒
- 支持审批节点
- 支持审批通过/拒绝
- 支持查看处理日志

### 状态建议
- 待分派
- 待处理
- 处理中
- 待审批
- 已完成
- 已归档
- 已关闭

### 权限点
- `Mail.Assign`
- `Mail.Transfer`
- `Mail.Comment.Internal`
- `Mail.Approval.Submit`
- `Mail.Approval.Process`

---

## 1.2.4 业务关联

### 功能点
- 自动按邮箱地址匹配联系人
- 手工关联客户/供应商/订单/合同/工单
- 展示业务对象的全部往来邮件
- 从业务对象详情页发起邮件
- 邮件线程绑定多个业务对象
- 支持“主关联对象 + 次关联对象”

### 权限点
- `BusinessRelation.View`
- `BusinessRelation.Create`
- `BusinessRelation.Update`
- `BusinessRelation.Delete`

---

## 1.2.5 模板中心

### 功能点
- 新建模板
- 模板分类管理
- 富文本编辑
- 模板变量插入
- 模板预览
- 模板版本管理
- 模板启用/停用
- 模板审批（可选）

### 变量示例
- `{{CustomerName}}`
- `{{OrderNo}}`
- `{{ContractNo}}`
- `{{Amount}}`
- `{{SenderName}}`
- `{{CurrentDate}}`

### 权限点
- `MailTemplate.View`
- `MailTemplate.Create`
- `MailTemplate.Update`
- `MailTemplate.Delete`
- `MailTemplate.Publish`

---

## 1.2.6 规则中心

### 功能点
- 新建规则
- 设置优先级
- 设置触发条件
- 设置执行动作
- 启停规则
- 查看规则命中日志
- 导入/导出规则

### 规则触发条件示例
- 发件人包含某域名
- 主题包含关键字
- 收件邮箱为某共享邮箱
- 附件类型为 PDF / XLSX
- 正文包含合同编号/订单号模式

### 动作示例
- 自动打标签
- 自动归档
- 自动分派
- 自动标记紧急
- 转入垃圾箱
- 自动创建工单（预留）

### 权限点
- `MailRule.View`
- `MailRule.Create`
- `MailRule.Update`
- `MailRule.Delete`
- `MailRule.Enable`
- `MailRule.Log.View`

---

# 2. ABP 模块划分与项目分层设计

---

## 2.1 总体架构建议

建议采用：

**模块化单体 + 清晰领域边界 + 后台任务异步化 + 对外 API 开放**

原因：

- 一期交付快
- 复杂度可控
- 适合 ABP 架构
- 后续可平滑拆分模块为独立服务

---

## 2.2 解决方案结构建议

```text
src
├─ MailManagement.Domain.Shared
├─ MailManagement.Domain
├─ MailManagement.Application.Contracts
├─ MailManagement.Application
├─ MailManagement.EntityFrameworkCore
├─ MailManagement.HttpApi
├─ MailManagement.HttpApi.Client
├─ MailManagement.DbMigrator
├─ MailManagement.Web
```

如果希望边界更清晰，建议按业务模块拆分为子模块：

```text
modules
├─ MailManagement.Identity
├─ MailManagement.Organization
├─ MailManagement.MailAccounts
├─ MailManagement.MailCore
├─ MailManagement.MailWorkflow
├─ MailManagement.MailTemplates
├─ MailManagement.MailRules
├─ MailManagement.MailRelations
├─ MailManagement.MailAttachments
├─ MailManagement.MailIntegration
├─ MailManagement.MailAudit
├─ MailManagement.MailReporting
```

---

## 2.3 模块职责划分

## 2.3.1 Identity / Organization 模块
### 职责
- 用户、角色、权限
- 组织、部门、岗位
- 数据权限范围
- 邮箱账号授权范围

### 依赖
- ABP Identity
- ABP PermissionManagement
- ABP TenantManagement（如预留多租户）

---

## 2.3.2 MailAccounts 模块
### 职责
- 邮箱账号管理
- IMAP/SMTP 配置
- OAuth2 授权信息
- 邮箱健康检查
- 同步策略配置

### 核心实体
- `MailAccount`
- `MailAccountCredential`
- `MailAccountSyncPolicy`
- `MailAccountHealthLog`

---

## 2.3.3 MailCore 模块
### 职责
- 邮件主实体管理
- 收件、发件、草稿、归档
- 邮件线程聚合
- 收件人信息
- 正文内容
- 状态管理

### 核心实体
- `MailMessage`
- `MailThread`
- `MailRecipient`
- `MailBody`
- `MailFolderMapping`

---

## 2.3.4 MailWorkflow 模块
### 职责
- 分派
- 转派
- 内部备注
- 处理状态流转
- 审批流程
- 待办提醒

### 核心实体
- `MailAssignment`
- `MailInternalComment`
- `MailProcessingRecord`
- `MailApproval`
- `MailTodo`

---

## 2.3.5 MailTemplates 模块
### 职责
- 模板管理
- 模板分类
- 模板变量
- 签名管理
- 模板渲染

### 核心实体
- `MailTemplate`
- `MailTemplateVersion`
- `MailTemplateCategory`
- `MailTemplateVariable`
- `MailSignature`

---

## 2.3.6 MailRules 模块
### 职责
- 自动分类规则
- 自动分派规则
- 黑白名单
- 命中日志

### 核心实体
- `MailRule`
- `MailRuleCondition`
- `MailRuleAction`
- `MailRuleExecutionLog`
- `MailDomainPolicy`

---

## 2.3.7 MailRelations 模块
### 职责
- 联系人管理
- 业务对象关联
- 邮件与客户/订单/合同/工单绑定

### 核心实体
- `Contact`
- `BusinessObjectRelation`
- `BusinessObjectType`

---

## 2.3.8 MailAttachments 模块
### 职责
- 附件元数据管理
- 文件存储抽象
- 权限校验
- 下载审计

### 核心实体
- `MailAttachment`
- `AttachmentAccessLog`

### 存储建议
- 元数据进 PG
- 文件内容走 ABP BlobStoring 或对象存储

---

## 2.3.9 MailIntegration 模块
### 职责
- 对外 API
- Webhook
- 外部系统映射
- 幂等控制
- 回调日志

### 核心实体
- `IntegrationApp`
- `IntegrationWebhook`
- `IntegrationEventLog`
- `ExternalMapping`
- `IdempotencyRecord`

---

## 2.3.10 MailAudit 模块
### 职责
- 操作审计
- 配置变更审计
- 附件下载审计
- 敏感操作留痕

### 核心实体
- `MailAuditLog`
- `ConfigurationChangeLog`

---

## 2.3.11 MailReporting 模块
### 职责
- 收发统计
- 响应时效统计
- 失败任务统计
- 人员处理统计

### 建议
- 初期可直接聚合查询
- 后续可增加统计快照表

---

## 2.4 ABP 分层职责建议

| 层 | 职责 |
|---|---|
| Domain.Shared | 枚举、常量、权限名、DTO 基础定义 |
| Domain | 聚合根、领域服务、领域规则 |
| Application.Contracts | DTO、AppService 接口、权限定义 |
| Application | 应用服务、事务编排、查询服务 |
| EntityFrameworkCore | EF Core 映射、仓储实现 |
| HttpApi | REST API 控制器 |
| Web | 管理后台 UI |
| DbMigrator | 数据库迁移与初始化 |

---

## 2.5 后台任务设计建议

建议使用后台任务处理以下内容：

- 邮箱收件同步
- 发件队列处理
- 发件失败重试
- 审批超时提醒
- SLA 检查
- 归档清理
- 规则批量重算
- Webhook 异步回调

---

# 3. 数据库表结构设计初稿

> 说明：
>
> - 这里给出一期核心表建议
> - 命名建议 PostgreSQL 使用 `snake_case`
> - 所有表建议包含 ABP 审计字段：
>   - `id`
>   - `creation_time`
>   - `creator_id`
>   - `last_modification_time`
>   - `last_modifier_id`
>   - `is_deleted`
>   - `deleter_id`
>   - `deletion_time`

---

## 3.1 核心表总览

| 表名 | 说明 |
|---|---|
| mail_accounts | 邮箱账号 |
| mail_account_credentials | 邮箱认证信息 |
| mail_threads | 邮件线程 |
| mail_messages | 邮件主表 |
| mail_recipients | 邮件收件人 |
| mail_attachments | 邮件附件 |
| mail_tags | 邮件标签 |
| mail_message_tags | 邮件标签关系 |
| mail_templates | 邮件模板 |
| mail_template_versions | 模板版本 |
| mail_signatures | 邮件签名 |
| contacts | 联系人 |
| business_object_relations | 业务对象关联 |
| mail_assignments | 邮件分派 |
| mail_internal_comments | 内部备注 |
| mail_approvals | 邮件审批 |
| mail_send_tasks | 发件任务 |
| mail_sync_tasks | 同步任务 |
| mail_rules | 规则主表 |
| mail_rule_conditions | 规则条件 |
| mail_rule_actions | 规则动作 |
| mail_rule_execution_logs | 规则执行日志 |
| mail_audit_logs | 邮件审计日志 |
| integration_apps | 集成应用 |
| integration_event_logs | 集成事件日志 |
| idempotency_records | 幂等记录 |

---

## 3.2 关键表设计

## 3.2.1 `mail_accounts`

```sql
create table mail_accounts (
    id uuid primary key,
    tenant_id uuid null,
    organization_unit_id uuid null,
    name varchar(128) not null,
    email_address varchar(256) not null,
    display_name varchar(128) null,
    account_type smallint not null, -- 1个人邮箱 2共享邮箱 3部门邮箱 4系统邮箱
    inbound_protocol smallint not null, -- 1IMAP 2POP3
    outbound_protocol smallint not null, -- 1SMTP 2API
    imap_host varchar(256) null,
    imap_port int null,
    smtp_host varchar(256) null,
    smtp_port int null,
    use_ssl boolean not null default true,
    sync_enabled boolean not null default true,
    send_enabled boolean not null default true,
    sync_interval_seconds int not null default 300,
    last_sync_time timestamp null,
    last_sync_status smallint null, -- 1成功 2失败
    health_status smallint null, -- 1正常 2告警 3不可用
    description varchar(512) null
);
create unique index idx_mail_accounts_email_address on mail_accounts(email_address);
```

---

## 3.2.2 `mail_account_credentials`

```sql
create table mail_account_credentials (
    id uuid primary key,
    mail_account_id uuid not null,
    auth_type smallint not null, -- 1密码 2OAuth2 3授权码
    user_name varchar(256) null,
    encrypted_password text null,
    encrypted_access_token text null,
    encrypted_refresh_token text null,
    token_expire_time timestamp null,
    extra_properties jsonb null
);
create index idx_mail_account_credentials_account_id on mail_account_credentials(mail_account_id);
```

---

## 3.2.3 `mail_threads`

```sql
create table mail_threads (
    id uuid primary key,
    tenant_id uuid null,
    mail_account_id uuid not null,
    subject varchar(512) null,
    normalized_subject varchar(512) null,
    thread_key varchar(256) not null,
    latest_message_time timestamp null,
    latest_sender varchar(256) null,
    message_count int not null default 0,
    unread_count int not null default 0,
    status smallint not null default 1, -- 1待处理 2处理中 3已完成 4已归档
    owner_user_id uuid null,
    priority smallint not null default 1 -- 1普通 2重要 3紧急
);
create index idx_mail_threads_thread_key on mail_threads(thread_key);
create index idx_mail_threads_owner_user_id on mail_threads(owner_user_id);
```

---

## 3.2.4 `mail_messages`

```sql
create table mail_messages (
    id uuid primary key,
    tenant_id uuid null,
    mail_account_id uuid not null,
    thread_id uuid null,
    folder_type smallint not null, -- 1收件箱 2已发送 3草稿 4归档 5垃圾箱
    internet_message_id varchar(512) null,
    in_reply_to varchar(512) null,
    references_text text null,
    subject varchar(512) null,
    normalized_subject varchar(512) null,
    from_address varchar(256) null,
    from_name varchar(256) null,
    sender_address varchar(256) null,
    reply_to varchar(256) null,
    body_format smallint not null, -- 1Text 2Html
    body_content text null,
    body_preview varchar(1000) null,
    sent_time timestamp null,
    received_time timestamp null,
    mail_direction smallint not null, -- 1接收 2发送
    read_status smallint not null default 0, -- 0未读 1已读
    processing_status smallint not null default 1,
    approval_status smallint not null default 0,
    importance smallint not null default 1,
    has_attachments boolean not null default false,
    size_in_bytes bigint not null default 0,
    external_message_uid varchar(256) null,
    raw_headers text null,
    is_starred boolean not null default false,
    is_locked boolean not null default false
);
create index idx_mail_messages_thread_id on mail_messages(thread_id);
create index idx_mail_messages_account_id on mail_messages(mail_account_id);
create index idx_mail_messages_received_time on mail_messages(received_time);
create index idx_mail_messages_processing_status on mail_messages(processing_status);
create index idx_mail_messages_internet_message_id on mail_messages(internet_message_id);
```

---

## 3.2.5 `mail_recipients`

```sql
create table mail_recipients (
    id uuid primary key,
    mail_message_id uuid not null,
    recipient_type smallint not null, -- 1To 2Cc 3Bcc
    email_address varchar(256) not null,
    display_name varchar(256) null
);
create index idx_mail_recipients_message_id on mail_recipients(mail_message_id);
create index idx_mail_recipients_email on mail_recipients(email_address);
```

---

## 3.2.6 `mail_attachments`

```sql
create table mail_attachments (
    id uuid primary key,
    tenant_id uuid null,
    mail_message_id uuid not null,
    file_name varchar(512) not null,
    file_extension varchar(32) null,
    content_type varchar(128) null,
    size_in_bytes bigint not null,
    file_hash varchar(128) null,
    blob_container varchar(128) null,
    blob_name varchar(512) not null,
    storage_type smallint not null, -- 1本地 2对象存储 3网络存储
    is_inline boolean not null default false,
    access_level smallint not null default 1 -- 1普通 2敏感 3仅授权
);
create index idx_mail_attachments_message_id on mail_attachments(mail_message_id);
create index idx_mail_attachments_blob_name on mail_attachments(blob_name);
```

---

## 3.2.7 `contacts`

```sql
create table contacts (
    id uuid primary key,
    tenant_id uuid null,
    contact_type smallint not null, -- 1客户联系人 2供应商联系人 3内部员工 4其他
    name varchar(128) not null,
    email_address varchar(256) not null,
    mobile varchar(64) null,
    company_name varchar(256) null,
    external_id varchar(128) null,
    source_system varchar(64) null,
    remark varchar(512) null
);
create index idx_contacts_email on contacts(email_address);
create index idx_contacts_external_id on contacts(external_id);
```

---

## 3.2.8 `business_object_relations`

```sql
create table business_object_relations (
    id uuid primary key,
    tenant_id uuid null,
    mail_message_id uuid null,
    thread_id uuid null,
    relation_type smallint not null, -- 1客户 2供应商 3订单 4合同 5工单 6项目 7发票 99自定义
    business_object_type varchar(64) not null,
    business_object_id varchar(128) not null,
    business_object_no varchar(128) null,
    business_object_name varchar(256) null,
    is_primary boolean not null default false,
    source_type smallint not null, -- 1自动匹配 2人工关联 3接口写入
    source_system varchar(64) null
);
create index idx_business_object_relations_message_id on business_object_relations(mail_message_id);
create index idx_business_object_relations_thread_id on business_object_relations(thread_id);
create index idx_business_object_relations_object on business_object_relations(business_object_type, business_object_id);
```

---

## 3.2.9 `mail_assignments`

```sql
create table mail_assignments (
    id uuid primary key,
    tenant_id uuid null,
    thread_id uuid not null,
    mail_message_id uuid null,
    from_user_id uuid null,
    to_user_id uuid null,
    to_role_id uuid null,
    to_organization_unit_id uuid null,
    assignment_type smallint not null, -- 1分派 2转派 3认领
    status smallint not null default 1, -- 1待接收 2处理中 3完成
    assigned_time timestamp not null,
    accepted_time timestamp null,
    finished_time timestamp null,
    comment varchar(512) null
);
create index idx_mail_assignments_thread_id on mail_assignments(thread_id);
create index idx_mail_assignments_to_user_id on mail_assignments(to_user_id);
```

---

## 3.2.10 `mail_internal_comments`

```sql
create table mail_internal_comments (
    id uuid primary key,
    tenant_id uuid null,
    thread_id uuid not null,
    mail_message_id uuid null,
    user_id uuid not null,
    content text not null,
    is_pinned boolean not null default false
);
create index idx_mail_internal_comments_thread_id on mail_internal_comments(thread_id);
```

---

## 3.2.11 `mail_approvals`

```sql
create table mail_approvals (
    id uuid primary key,
    tenant_id uuid null,
    mail_message_id uuid not null,
    approval_code varchar(64) not null,
    node_name varchar(128) null,
    approver_user_id uuid null,
    approval_status smallint not null default 0, -- 0待审批 1通过 2拒绝 3撤回
    submitted_user_id uuid not null,
    submitted_time timestamp not null,
    processed_time timestamp null,
    comment varchar(1000) null
);
create index idx_mail_approvals_message_id on mail_approvals(mail_message_id);
create index idx_mail_approvals_approver_user_id on mail_approvals(approver_user_id);
```

---

## 3.2.12 `mail_send_tasks`

```sql
create table mail_send_tasks (
    id uuid primary key,
    tenant_id uuid null,
    mail_account_id uuid not null,
    mail_message_id uuid null,
    task_no varchar(64) not null,
    send_status smallint not null default 0, -- 0待发送 1发送中 2成功 3失败 4取消
    retry_count int not null default 0,
    max_retry_count int not null default 3,
    scheduled_send_time timestamp null,
    last_send_time timestamp null,
    next_retry_time timestamp null,
    last_error_message varchar(2000) null,
    triggered_by_type smallint not null, -- 1人工 2系统 3接口
    external_request_id varchar(128) null
);
create unique index idx_mail_send_tasks_task_no on mail_send_tasks(task_no);
create index idx_mail_send_tasks_status on mail_send_tasks(send_status);
```

---

## 3.2.13 `mail_rules`

```sql
create table mail_rules (
    id uuid primary key,
    tenant_id uuid null,
    name varchar(128) not null,
    rule_type smallint not null, -- 1分类 2分派 3安全 4归档
    priority int not null default 100,
    is_enabled boolean not null default true,
    stop_on_match boolean not null default false,
    applies_to_account_id uuid null,
    applies_to_folder_type smallint null,
    description varchar(512) null
);
create index idx_mail_rules_priority on mail_rules(priority);
```

---

## 3.2.14 `mail_rule_conditions`

```sql
create table mail_rule_conditions (
    id uuid primary key,
    rule_id uuid not null,
    field_name varchar(64) not null, -- from_address/subject/body/attachment_ext...
    operator varchar(32) not null, -- contains/equals/regex/in/notin
    compare_value text not null,
    logical_operator varchar(8) null -- AND/OR
);
create index idx_mail_rule_conditions_rule_id on mail_rule_conditions(rule_id);
```

---

## 3.2.15 `mail_rule_actions`

```sql
create table mail_rule_actions (
    id uuid primary key,
    rule_id uuid not null,
    action_type varchar(64) not null, -- add_tag/assign/archive/mark_important/reject
    action_value text null,
    action_order int not null default 1
);
create index idx_mail_rule_actions_rule_id on mail_rule_actions(rule_id);
```

---

## 3.2.16 `mail_audit_logs`

```sql
create table mail_audit_logs (
    id uuid primary key,
    tenant_id uuid null,
    user_id uuid null,
    user_name varchar(128) null,
    operation_type varchar(64) not null,
    entity_type varchar(64) not null,
    entity_id varchar(128) not null,
    mail_message_id uuid null,
    thread_id uuid null,
    description varchar(1000) null,
    ip_address varchar(64) null,
    client_type varchar(64) null,
    extra_properties jsonb null,
    operation_time timestamp not null
);
create index idx_mail_audit_logs_mail_message_id on mail_audit_logs(mail_message_id);
create index idx_mail_audit_logs_user_id on mail_audit_logs(user_id);
create index idx_mail_audit_logs_operation_time on mail_audit_logs(operation_time);
```

---

## 3.3 索引与数据策略建议

### 索引建议
重点建立以下索引：

- `mail_messages(received_time)`
- `mail_messages(thread_id)`
- `mail_messages(internet_message_id)`
- `mail_recipients(email_address)`
- `contacts(email_address)`
- `business_object_relations(business_object_type, business_object_id)`
- `mail_send_tasks(send_status, next_retry_time)`

### 数据策略建议
- 邮件正文允许存数据库
- 大附件必须独立存储
- 发件任务、同步任务、规则日志可定期归档
- 审计日志建议保留长期
- 如邮件量大，可按月份分区 `mail_messages`

---

# 4. API 接口设计规范（用于 ERP/CRM 集成）

---

## 4.1 API 设计原则

- REST 风格
- 接口版本化：`/api/mail-management/v1/...`
- 统一返回结构
- 支持幂等
- 支持审计与链路追踪
- 接口调用方使用 `AppId + AppSecret` 或 OAuth2

---

## 4.2 统一返回结构建议

```json
{
  "code": "0",
  "message": "success",
  "data": {},
  "traceId": "00-xxx-xxx-01",
  "timestamp": "2026-04-22T10:00:00Z"
}
```

---

## 4.3 核心接口清单

## 4.3.1 创建待发邮件

### 接口
`POST /api/mail-management/v1/mails/send-tasks`

### 用途
ERP/CRM 创建发件任务，由邮件系统异步处理。

### 请求示例
```json
{
  "requestId": "crm-20260422-0001",
  "mailAccountId": "11111111-1111-1111-1111-111111111111",
  "subject": "订单确认通知 - SO20260422001",
  "bodyFormat": "Html",
  "bodyContent": "<p>尊敬的客户，您的订单已确认。</p>",
  "to": [
    {
      "email": "customer@demo.com",
      "name": "张三"
    }
  ],
  "cc": [],
  "bcc": [],
  "templateCode": "ORDER_CONFIRM",
  "templateVariables": {
    "CustomerName": "张三",
    "OrderNo": "SO20260422001",
    "Amount": "20000"
  },
  "businessRelations": [
    {
      "type": "Order",
      "id": "SO20260422001",
      "name": "销售订单"
    },
    {
      "type": "Customer",
      "id": "CUS0001",
      "name": "上海某客户"
    }
  ],
  "attachments": [
    {
      "fileToken": "upload-token-001",
      "fileName": "订单确认单.pdf"
    }
  ],
  "scheduledSendTime": null,
  "needApproval": false
}
```

### 返回示例
```json
{
  "code": "0",
  "message": "success",
  "data": {
    "taskId": "22222222-2222-2222-2222-222222222222",
    "taskNo": "SEND202604220001",
    "status": "Pending"
  }
}
```

---

## 4.3.2 查询发件任务状态

### 接口
`GET /api/mail-management/v1/mails/send-tasks/{taskId}`

### 返回字段建议
- `taskId`
- `taskNo`
- `status`
- `retryCount`
- `lastErrorMessage`
- `lastSendTime`
- `mailMessageId`

---

## 4.3.3 查询邮件详情

### 接口
`GET /api/mail-management/v1/mails/{id}`

### 返回字段
- 邮件基本信息
- 收件人
- 正文
- 附件
- 当前状态
- 线程 ID
- 业务关联信息
- 审批状态

---

## 4.3.4 查询线程详情

### 接口
`GET /api/mail-management/v1/threads/{id}`

### 用途
CRM/ERP 查看完整往来上下文。

### 返回字段
- 线程主题
- 线程状态
- 负责人
- 邮件列表
- 内部备注列表
- 业务对象关联列表

---

## 4.3.5 按业务对象查询邮件

### 接口
`GET /api/mail-management/v1/business-relations/mails`

### 查询参数
- `businessObjectType`
- `businessObjectId`
- `pageNo`
- `pageSize`

### 示例
`GET /api/mail-management/v1/business-relations/mails?businessObjectType=Order&businessObjectId=SO20260422001`

---

## 4.3.6 手工关联业务对象

### 接口
`POST /api/mail-management/v1/mails/{id}/relations`

### 请求示例
```json
{
  "relations": [
    {
      "businessObjectType": "Contract",
      "businessObjectId": "HT2026001",
      "businessObjectName": "采购合同",
      "isPrimary": true
    }
  ]
}
```

---

## 4.3.7 Webhook 事件回调

### 事件建议
- `mail.received`
- `mail.sent`
- `mail.send.failed`
- `mail.approval.approved`
- `mail.approval.rejected`
- `mail.thread.updated`
- `mail.assignment.created`

### 回调示例
```json
{
  "eventName": "mail.sent",
  "eventId": "evt-001",
  "occurredTime": "2026-04-22T11:00:00Z",
  "data": {
    "taskId": "22222222-2222-2222-2222-222222222222",
    "mailMessageId": "33333333-3333-3333-3333-333333333333",
    "businessRelations": [
      {
        "type": "Order",
        "id": "SO20260422001"
      }
    ],
    "status": "Success"
  }
}
```

---

## 4.4 幂等策略建议

对以下接口建议要求传入 `requestId`：

- 创建发件任务
- 同步业务对象
- 写入邮件关联
- 回调确认接口

服务端建立 `idempotency_records` 表，按：
- 调用方 `app_id`
- `request_id`
- 接口路径
- 请求摘要

做幂等控制。

---

# 5. 一期实施范围与开发排期建议

---

## 5.1 一期目标

一期目标不是把所有 ERP/CRM 功能做完，而是先做成一个：

- 可正常收发邮件
- 可统一管理多个邮箱
- 可做协同分派
- 可做基础业务关联
- 可开放接口集成
- 可审计、可维护、可扩展

的**企业级邮件管理基础平台**。

---

## 5.2 一期建议范围（MVP）

## 必做范围
1. 组织、用户、角色、权限
2. 邮箱账号接入与管理
3. 邮件收件同步
4. 邮件发送与重试
5. 邮件列表/详情/线程查看
6. 附件上传下载
7. 标签与基础规则
8. 分派与内部备注
9. 模板与签名
10. 联系人与业务对象关联
11. 审计日志
12. 对外 API（基础版）

## 可选增强
1. 发件审批
2. Webhook 回调
3. 高级统计报表
4. 自动工单转换

---

## 5.3 迭代建议

## 迭代 1：基础框架搭建（2 周）
### 内容
- ABP 解决方案初始化
- PostgreSQL 与 EF Core 配置
- Identity / Permission 接入
- 基础模块骨架搭建
- 公共异常、审计、日志、配置中心

### 输出
- 可运行基础工程
- 模块骨架
- CI/CD 初版
- 数据库迁移基线

---

## 迭代 2：邮箱账号与邮件核心（3 周）
### 内容
- 邮箱账号配置
- IMAP 拉取
- SMTP 发送
- 邮件入库
- 收件箱/已发送/草稿/归档
- 线程聚合
- 附件元数据管理

### 输出
- 基础邮件收发闭环
- 邮件列表/详情页面
- 邮件同步任务

---

## 迭代 3：协同处理与模板（2 周）
### 内容
- 分派
- 内部备注
- 状态流转
- 模板管理
- 签名管理
- 标签与基础规则

### 输出
- 协同处理闭环
- 模板发件能力
- 规则自动打标签/分派

---

## 迭代 4：业务关联与开放接口（2 周）
### 内容
- 联系人管理
- 业务对象关联
- 查询业务对象关联邮件
- 创建待发邮件 API
- 查询邮件与线程 API
- 幂等控制

### 输出
- ERP/CRM 可初步接入
- 业务关联邮件展示能力

---

## 迭代 5：审计、报表与上线准备（2 周）
### 内容
- 审计日志
- 基础统计报表
- 健康检查
- 性能优化
- 安全测试
- UAT 支持

### 输出
- 上线版本
- 部署文档
- 运维手册
- 验收资料

---

## 5.4 总工期建议

| 阶段 | 周期 |
|---|---|
| 基础架构 | 2 周 |
| 邮件核心 | 3 周 |
| 协同模板 | 2 周 |
| 业务集成 | 2 周 |
| 审计上线 | 2 周 |
| **合计** | **11 周左右** |

> 若团队配置合理，一期 MVP 可控制在 **10~12 周**。

---

## 5.5 团队配置建议

| 角色 | 人数建议 | 说明 |
|---|---:|---|
| 产品经理/业务分析 | 1 | 梳理业务与验收 |
| 架构师/技术负责人 | 1 | 设计架构与关键方案 |
| 后端开发 | 2~4 | ABP + 邮件协议 + 接口 |
| 前端开发 | 1~2 | 管理后台与邮件工作台 |
| 测试工程师 | 1~2 | 功能、接口、性能、安全 |
| 运维/DevOps | 1 | 部署、监控、CI/CD |

---

# 6. 技术实现建议补充

---

## 6.1 邮件协议建议

### 推荐
- 收件：`IMAP`
- 发件：`SMTP`

### 原因
- IMAP 更适合企业级同步与多文件夹管理
- SMTP 兼容性高
- POP3 可作为兼容选项，但不建议作为主协议

---

## 6.2 正文与附件处理建议

### 正文
- HTML 正文需做安全过滤
- 保留原始 HTML
- 生成安全展示版 HTML
- 生成纯文本预览字段

### 附件
- 仅存元数据到数据库
- 文件内容进入 Blob/ObjectStorage
- 附件下载必须鉴权
- 敏感附件下载必须审计

---

## 6.3 搜索建议

### 一期
- PostgreSQL 全文检索
- 主题、正文预览、发件人、收件人复合检索

### 二期
- 如邮件量增长明显，可接入 Elasticsearch / OpenSearch

---

## 6.4 审计建议

关键动作必须记录：

- 查看邮件
- 下载附件
- 删除邮件
- 导出邮件
- 修改规则
- 修改邮箱配置
- 审批邮件
- 接口调用发件

---

# 7. 风险与落地建议

## 7.1 主要风险

### 风险 1：不同邮箱服务商兼容差异
**建议：**
- 抽象邮箱适配层
- 先兼容标准 IMAP/SMTP
- 后续扩展 Exchange / Graph API

### 风险 2：线程归并不准确
**建议：**
- 优先使用 `Message-Id` / `In-Reply-To` / `References`
- 主题仅作为辅助条件

### 风险 3：邮件量大后查询性能下降
**建议：**
- 建好索引
- 分页查询
- 归档策略
- 预留分区表和搜索引擎扩展

### 风险 4：权限复杂导致越权
**建议：**
- 邮箱权限、邮件权限、附件权限分层设计
- 所有下载与查看均做权限检查

---

# 8. 建议的下一步输出顺序

如果你准备进入实际立项或开发阶段，我建议下一步按以下顺序继续细化：

1. **产品原型与页面结构说明**
2. **数据库 ER 图 + 全量字段字典**
3. **详细接口文档（OpenAPI/Swagger 风格）**
4. **ABP 解决方案代码结构模板**
5. **领域模型设计（聚合、实体、枚举、仓储接口）**
6. **开发任务拆分 WBS**
7. **一期验收标准文档**

---

# 9. 结论

对于你的场景，这套系统最合适的落地方向是：

- **不是单纯邮箱客户端**
- 而是一个 **邮件业务中台 / 企业通信协同模块**
- 一期以 **模块化单体** 为主
- 重点先打通：
  - 邮件收发
  - 协同处理
  - 模板规则
  - 业务关联
  - 开放接口
  - 审计安全

这样后续无论是：

- 拓展为 ERP 子模块
- 拓展为 CRM 沟通中心
- 作为独立邮件管理系统部署
- 对接第三方 ERP/CRM

都具备较好的可持续演进能力。

---

如果你需要，我下一步可以直接继续输出以下任意一个完整文档：

## 可继续输出的内容
1. **数据库 ER 图说明 + 全字段数据字典**
2. **ABP 项目目录结构与模块依赖图**
3. **详细 API 文档（Swagger/OpenAPI 风格）**
4. **前后端菜单与权限点清单**
5. **开发任务分解 WBS + 人天估算**
6. **系统原型页面说明文档**
7. **领域模型设计文档（聚合根/实体/枚举）**

如果你愿意，我可以直接下一步继续写：

> **《邮件管理系统数据库设计文档（详细版）》**

或者

> **《邮件管理系统 ABP 架构设计文档》**

你回复一个即可，我直接接着输出。
